# 개발/테스트 전용. 이식 대상이 아니다.
-r requirements.txt

pytest>=8.0
httpx>=0.27          # fastapi.testclient
openpyxl>=3.1        # db/migrate.py 의 엑셀 파싱
# 저장소 루트 `.env` 로더. **requirements.txt 에 넣지 않는다** — 호스트에 이식되는
# 라이브러리 코드는 파일에서 설정을 읽지 않는다 (INTEGRATION.md §4). 읽는 곳은
# 개발 전용 진입점뿐이고, 없어도 동작한다 (import 실패 시 건너뛴다).
python-dotenv>=1.0
